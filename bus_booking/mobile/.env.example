# Django API (required on physical devices — use your PC's LAN IP, e.g. http://192.168.1.10:8000)
# Android emulator defaults to http://10.0.2.2:8000 in dev when this is unset.
# iOS Simulator defaults to http://127.0.0.1:8000
EXPO_PUBLIC_API_URL=
