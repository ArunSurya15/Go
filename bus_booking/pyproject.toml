[project]
name = "bus-booking-backend"
version = "0.1.0"
description = "Django API for bus booking (install deps via requirements.txt)"
requires-python = ">=3.12,<3.15"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["backend"]
